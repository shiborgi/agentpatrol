Apply installed React and framework conventions to state ownership, rendering
boundaries, and observable interaction behavior. Preserve established styling;
check keyboard, loading, error, focus, and responsive states. Use effects only
for external synchronization and add memoization only with measured need.
